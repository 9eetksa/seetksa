# استلام مشروع صيت — تبديل حساب كودكس

آخر تحديث: 2026-09-23 بتوقيت الرياض

## الإنتاج

- أُنشئت واجهة سينمائية مختصرة في `src/home/CinemaHome.jsx` بتاريخ2026-09-23 مع حكاية تصوير/مونتاج/إخراج من مشهدين؛ الأصل متاح عبر `/?design=classic` ومحفوظ بالوسم المرفوع `seet-home-before-cinema-2026-09-23`؛ أدلة الفحص والنشر في سجل المهمة

- أُصلح الدخول بالنيابة لحسابات الجوال في2026-09-23 ونُشر كود54da30c: ترحيل102 `20260923120000_seet_phone_impersonation.sql`، متغير SUPABASE_JWT_SECRET حساس بالخادم، دعم RPCs المهام والمرفقات؛ تحقق API الإنتاج ناجح وأزيلت حسابات QA؛ تفاصيل الصلاحيات والأدلة في سجل المهمة

- المنصة منشورة على `https://seetksa.vercel.app/login` في مشروع Vercel `seetksa`؛ متغيرات الإنتاج وقاعدة صيت وGreen API وwebhook وdispatch مرتبطة ومتحقق منها، والتفاصيل في `docs/task-state.md`
- إعداد Supabase Site URL ورابط استعادة كلمة المرور محدثان للإنتاج؛ لا تعِد إرسال رموز المستخدم أو إنشاء الحسابات المكتملة
- GitHub: رُفعت التحديثات بنجاح في2026-09-23؛ helper محلي `.tools/git-credential-seet.cjs` يختار اعتماد `9eetksa` صراحة من gh دون تخزين المفتاح، فلا يتأثر بتبديل حساب gh النشط؛ الرفع التالي `git push` دون إعادة init أو إضافة origin
- الربط التلقائي مع Vercel رُفض بصلاحيات تكامل GitHub، وهو مستقل عن نجاح رفع Git والنشر المباشر

## أحدث استلام — مهام الفريق والدخول بالجوال

- أُنجز مسار مهام الفريق والإشعارات والتوزيع والانضمام والمسودات والمرفقات الخاصة والكانبان والتقويم مع إبقاء الميزات السابقة؛ فحصا المهام والمسودات الحيان ناجحان وجميع حسابات QA حذفت
- نافذة الحسابات بيضاء بهوية صيت: متعاون وموظف وأدمن ومشرف؛ المشرف مسؤول عمليات؛ إنشاء الحساب بالجوال دون بريد مع تعيين كلمة مرور عند أول دخول
- الدخول النهائي Supabase Auth أصلي: كلمة مرور أو OTP من 6 أرقام عبر Green API، صلاحية300 ثانية وإعادة إرسال60 ثانية؛ Phone وSend SMS Hook مفعّلان، signup العام مغلق؛ لا تستخدم مسار المعرفات البريدية البديلة الذي استُبعد
- القناة المستقلة لصيت مربوطة الآن: أضاف المستخدم GREEN_API_TOKEN وحُفظ في Vault عبر `.tools/configure-seet-whatsapp.mjs`؛ فحص القناة authorized=true وطلب OTP للحساب الذي أرسله المستخدم بالصورة قُبل دون خطأ؛ المتبقي تأكيد وصول الرسالة للهاتف، ولا تطبع المفتاح أو تعِد إرسال الرموز تلقائيا
- آخر ترحيل101 `20260921130923_seet_account_type_auth_update.sql` يعالج أن Auth يكتب metadata بعد الإدراج؛ تهيئة صلاحية المشرف لا تعيد منح صلاحية سبق سحبها
- آخر اختبارات محلية101 ترحيل ناجحة في2.31s، حسابات/دعوات/انتحال29 ناجحة، البناء النهائي ناجح في6.41s؛ التفاصيل والأدلة وحدود صلاحيتها في `docs/task-state.md`
- الفحص الحي النهائي للحسابات نجح بعد101 عند13:11:32Z في35.65s؛ `.tools/phone-onboarding-live-check.json` يثبت الدخول بالجوال وتغيير كلمة المرور والصلاحيات وحذف البيانات المؤقتة دون تغيير الحسابات الأصلية؛ لم تُرسل رسائل خارجية ولم يُثبت وصول OTP لغياب مفتاح القناة
- البنود التاريخية أدناه توثق الحالة السابقة؛ هذه الفقرة و`docs/task-state.md` يحددان الوضع الحالي

هذا الملف لاستكمال العمل عند تبديل **حساب Codex بين عبدالله وأحمد ومودي** على مساحة المشروع نفسها وليس لتبديل حسابات مستخدمي منصة صيت
لا توجد آلية تبديل حسابات تلقائية ولم يُنفذ تسجيل خروج أو تغيير حساب من هذه الجلسة

## ابدأ من هنا

1. اقرأ `AGENTS.md` ثم ملخص `docs/task-state.md` والنتائج المرتبطة بالطلب الجديد فقط
2. أكمل من آخر نتيجة موثقة ولا تعد استكشاف المشروع أو تشغيل الفحوصات الناجحة دون تغير مؤثر
3. ملفات مساحة العمل هي المرجع المشترك بين الحسابات على هذا الجهاز ولا تفترض انتقال الأدوات أو جلسات Chrome أو الملفات إلى جهاز آخر أو CODEX_HOME آخر

## الحالة الحالية

- المسار `C:\Projects\seet` والتقنية React 18 / Vite 6 / JavaScript / Supabase / GSAP
- المعاينة المحلية `http://127.0.0.1:5175` وكانت تعمل عند آخر تحقق؛ لا تشغّل خادما مكررا إذا كانت متاحة
- المشروع ليس مستودع Git مهيأ وقت العمل الأخير؛ لا تستخدم reset/checkout لاسترجاع حالة مفترضة
- `C:\Projects\pro` مرجع برو فيجن للقراءة فقط ولا تُنقل منه الأسرار أو حسابات المستخدمين
- قاعدة صيت `hhdrtrrghntzhqrqdvlr` مفعلة مع **95 ترحيلا مطبقا** و22 جدولا عاما محميا و23 سياسة staff-only؛ لا تعِد تطبيق الترحيلات
- آخر ترحيل حي `20260921130923_seet_account_type_auth_update.sql` ومسجل في سجل ترحيلات Supabase
- إعدادات الاتصال وأسرار الخادم موجودة محليا في `.env.local`؛ لا تطبعها أو تنسخها إلى ملفات الاستلام أو كود المتصفح
- `.tools/seet-admin-access.txt` تسليم دخول مؤقت سابق وليس دليلا على كلمة المرور الحالية؛ لا تعِد إنشاء الحساب التقني أو تغيير كلمته
- لا تفترض صلاحية موصل Supabase للحساب الجديد؛ الموصل وCLI سابقا لم يملكا مشروع صيت بينما نجحت جلسة Chrome لملف seetksa وAuth/Storage APIs بالمفتاح المحلي
- التفعيل على قاعدة البيانات حقيقي؛ الواجهة تعمل محليا ولم يُنفذ نشر عام جديد

## المنجز الذي يجب الحفاظ عليه

- هوية صيت الرسمية والخطوط والأصول المحلية والشركاء الأصليون؛ **حركة «شاهد قصتنا» لا تتغير** وقسم شركاء النجاح ضروري
- المنصة للموظفين والمشرفين والإدارة؛ العملاء سجلات داخلية دون بوابة دخول مع حماية Auth/RPC/RLS/Storage
- ربط سير العمل ولوحات الإدارة والتوجيه والإشعارات والتخزين ونقل تحديثات برو فيجن مع تكييف دور المشرف لصيت
- إصلاح تعيين كلمة المرور الأولى بإكمال سجل account_invitations المفقود وتصحيح سكربت التهيئة دون تغيير كلمة المستخدم النهائية
- حذف الأعمال مع التأكيد عبر `api/portfolio.js` و`server/portfolio-service.mjs` وحذف الصور المرفوعة عبر Storage API؛ الرئيسية مرتبطة بالأعمال النشطة وتبقى أربع مساحات فارغة عند خلو المكتبة
- تحديث الأعمال فوري بين تبويبات الجهاز وعند إعادة فتح الصفحة؛ الزوار الآخرون يتحدث عرضهم كل 15 ثانية أثناء الظهور؛ الحاوية ما زالت خاصة
- إصلاح الصفحة البيضاء لمركز الإشعارات في `src/workflow/data.js`: لكل اشتراك Realtime topic مستقل بلاحقة UUID كي لا يتعارض الجرس مع المركز
- آخر طلب مكتمل: إضافة **تعديل المسمى وحذف القسم** إلى `src/workflow/DepartmentManager.jsx`؛ إعادة التسمية مستقلة عن تعيين الفريق والحذف يمنع فقد تاريخ الطلبات والمهام ويحفظ حسابات الموظفين
- عمليات الأقسام تستخدم RPC `work_department_identity` مع departments.manage والنسخة وقفل الصف وسجل الإدارة ولا تعتمد على إخفاء الأزرار للتفويض

## التحقق المنفذ — لا تكرره تلقائيا

التفاصيل والتوقيت وحدود إعادة الاستخدام في `docs/task-state.md`؛ هذه نتائج تاريخية وليست فحصا جديدا عند تبديل الحساب

- آخر بناء: `node node_modules/vite/bin/vite.js build` ناجح خلال 6.57s بعد تعديل الأقسام؛ الدليل `.tools/department-identity-build.log` وتحذير أحجام الحزم الموروث فقط
- `node tests/seet-database.mjs` ناجح: إعادة تشغيل 95 ترحيلا واختبارات الصلاحيات والتاريخ والحذف والتتابع وبقاء الموظفين
- التحقق الحي للأقسام ناجح في 2026-09-20T23:59:23.854Z؛ الدليل `.tools/department-identity-live-check.json` وشمل منح وسحب الصلاحية فورا وتعديل الاسم والحذف والنسخة والاسم المكرر
- `tests/notification-subscriptions.test.js` أعاد استثناء SDK قبل الإصلاح ونجح باختبارين بعده للجرس والمركز والإغلاق وإعادة الفتح
- اختبارات الأعمال والكتالوج والتحديث 22 ناجحة؛ `.tools/portfolio-delete-live-check.json` يثبت الحذف والصلاحيات والتتابع وحذف الصورة والتكرار الآمن
- `.tools/seet-first-password-check.json` يثبت نجاح دورة تغيير كلمة المرور بحساب اختباري
- حُذفت بيانات الاختبار ولم تُحذف أو تُعدّل أعمال المستخدم أو أقسامه الحالية أثناء التحقق
- مراجعات الواجهات الأخيرة من المصدر دون متصفح التطبيق؛ استخدام Chrome اقتصر على إدارة قاعدة صيت من الجلسة التي أتاحها المستخدم

## المتبقي فقط

- البريد وواتساب: لا توجد أسرار Resend/Green API لصيت بعد؛ سُئل المستخدم إن كانت القنوات مستقلة أو مشتركة مع برو فيجن ولم يصل جواب
- العامل work-notifications منشور والجدولة والسر الداخلي مهيأة؛ آخر تحقق رفض الطلب دون السر 401 وأعاد 503 not_configured بالسر الصحيح لغياب إعدادات المزود
- لا تدّع إرسال الرسائل أو وصولها ولا تنسخ أسرار برو فيجن؛ أكمل إعداد القنوات عند توفير معلوماتها ثم تحقق من الخدمة الحية
- النشر العام عند طلبه؛ لا تغيّر APP_URL وعناوين Auth المحلية إلى نطاق مفترض
- لا يوجد طلب برمجي آخر جارٍ أو فشل غير محلول في التعديلات الأربع الأخيرة؛ انتظر توجيه المستخدم التالي مع الحفاظ على المتبقي أعلاه

## تذكير تشغيلي

- أقل توكن ضروري مع اكتمال الجودة والأمان؛ حافظ على النموذج ومستوى التفكير ولا تفتح المتصفح تلقائيا أو تعِد البناء لمجرد قراءة الاستلام
- استخدم مهارات ECC وSupabase ذات الصلة وأضف مهارات التصميم عند عمل واجهة جوهرية وفقا لـ AGENTS.md
- استخدم `Get-Content -Encoding UTF8` و`apply_patch` للعربية؛ تمرير العربية عبر بعض أنابيب PowerShell إلى Node قد يفسد الترميز
- عند الحاجة المصرح بها إلى SQL Editor تجنب تغير LF إلى CRLF في SQL الذي يعدّل تعريفات دوال بالمطابقة النصية؛ النشر السابق حفظ النص بترميز Base64 ومعاملة واحدة مع تسجيل إصدار الترحيل
- عند اختبار الدخول سجّل خروج جلسة الاختبار بنطاق local حتى لا تقطع جلسة المستخدم
- حدّث السجل المعتمد عند تغير النتائج؛ هذا الملف ملخص استلام ولا يحل محل الأدلة أو يعطي صلاحيات جديدة
